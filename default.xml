<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- Upstream remotes -->
  <remote name="aosp"
          fetch="https://android.googlesource.com/" />

  <remote name="grapheneos"
          fetch="https://github.com/GrapheneOS/" />

  <remote name="github"
          fetch="https://github.com/" />

  <!-- Default settings -->
  <default revision="android-15.0.0_r36"
           remote="aosp"
           sync-j="4" />
           
  <default revision="15-qpr2"
           remote="grapheneos" />         

  <!-- GrapheneOS base (pulls in ~100 repos) -->
  <include name="platform_manifest.xml" />

  <!-- === SentientOS Custom Modules === -->

  <!-- AI log scanner module -->
  <!--project path="external/ai_sentient"
           name="SentientOS/ai_sentient"
           remote="github"
           revision="master" />

  <!-- OTA update server (optional for S3 integration) -->
  <!--project path="ota-server-go"
           name="SentientOS/ota-server-go"
           remote="github"
           revision="main" />

</manifest>

